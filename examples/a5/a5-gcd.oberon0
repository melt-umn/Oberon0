MODULE GCD;
  VAR x, y : INTEGER;
  PROCEDURE Gcd(VAR a : INTEGER; VAR b : INTEGER);
    VAR c: INTEGER;
    BEGIN
      WHILE b # 0 DO
        c := b;
        b := a MOD b;
        a := c
      END;
      Write(a); WriteLn
  END Gcd;
  BEGIN
    Read(x); Read(y);
    Gcd(x,y)
END GCD.